/** Placeholder até a feature real ser implementada — mantém o router
 * navegável enquanto cada tela é construída na sua fase (ver BACKLOG.md). */
export function PlaceholderPage({ title }: { title: string }) {
  return (
    <div>
      <h1>{title}</h1>
      <p>Em construção.</p>
    </div>
  );
}
